# HISTORY

コード・Gitでは追えない「なぜそうなったか」を記録する。詳細仕様は `auto_apo-app_仕様書_v1.md` を参照。

## 2026-07-01: corp-lead-kit 初期実装（M1：CSV取り込み＋DB）

- **依存ライブラリを一切追加していない**。`node -v` が v24.15.0（フルICU入り）だったため、`node:sqlite`（DatabaseSync）と `TextDecoder('shift-jis')` がいずれも標準機能で動作することを確認済み。iconv-lite等のShift-JIS変換ライブラリ、CSVパーサライブラリは不要と判断。
- **CSVの行分割は生バイトのLF(0x0A)単位で行い、その後にデコードする方式にした**。理由：LF・CR・カンマ・引用符（いずれも0x00〜0x3F/0x7Fの範囲）は、Shift-JISの2バイト文字の後続バイト範囲（0x40〜0xFC）・UTF-8の継続バイト範囲（0x80〜0xBF）のどちらにも含まれないため、デコード前に生バイトで区切っても文字の途中で切れることがない。これにより219MB級の全件CSVでも先頭から`--limit`件で早期終了でき、全件をメモリに載せる必要がない。
- **ZIP展開・OpenPGP署名検証はv1では実装していない**。国税庁の全件ダウンロードはZIP圧縮＋署名付きだが、展開・検証には追加ライブラリが必要になるため保留。現状は「ユーザーが手動でZIPを展開した後の.csvファイルを渡す」運用とする。必要になれば別途相談の上、ライブラリ追加を検討する。
- **テストのCSVフィクスチャはUTF-8で作成した**。Node標準のTextEncoderはUTF-8エンコードしかできず、Shift-JISへの変換ライブラリなしにShift-JISのテストデータを生成できないため。Shift-JISデコード自体は`TextDecoder('shift-jis').decode(...)`の単体確認で動作を確認済み（実運用でShift-JIS版CSVを使う場合は`encoding: 'shift-jis'`を指定すれば動く設計）。
- **列がズレた場合は即座にthrowして処理全体を中断する設計にした**（仕様書§3の指示通り）。法人番号13桁・都道府県コード1〜2桁・郵便番号7桁の自己検証をパース時に行い、不一致ならその場でエラーを投げる。サイレントに壊れたデータをDBに入れないことを優先。
- M2（サイト巡回）・M3（適格判定）・auto_apo-app（M4以降）は未実装。次回以降のセッションで着手する。

## 2026-07-01: corp-lead-kit M2実装（サイト巡回・情報抽出）

- **依存ライブラリを4点追加した**（ユーザー承認済み）：`cheerio`（HTML解析）、`playwright`（JS重いサイトのフォールバック描画）、`@anthropic-ai/sdk`（Web検索・要約・お断り判定）、`p-limit`（並列数制御）。追加前にユーザーに確認を取った。
- **公式サイトURLの特定は、外部の検索APIを別途契約せず、Claude APIのserver-side Web検索ツール（`web_search_20250305`）で行うことにした**。理由：Google Custom Search等の別APIキー契約が不要になり、既に導入予定だったAnthropic SDK 1つで「検索」「要約」「お断り判定」まで完結できるため。ユーザーにも選択肢を提示した上で承認を得た。
- **robots.txtのパーサは自作した**（専用ライブラリを追加していない）。User-agent別グループの最長一致（Disallow/Allowのうちパスが長く一致した方を採用）という標準的な解釈のみを実装。robots.txtの取得自体に失敗した場合（404・タイムアウト等）は「規約なし＝許可」として安全側ではなく処理継続側に倒しているが、これは「読めない規約で無限に足止めしない」ための実務判断。取得できて中身がDisallowだった場合は明確に不許可にする。
- **M2内でのステータス遷移を3系統に分けた**：
  - 公式サイトが見つからない／robots.txtで明確に禁止 → その場で`status=excluded`＋`exclude_reason`を記録（M3を待たずM2で確定除外）。
  - サイト取得自体が失敗（タイムアウト等の一時的エラー） → `status`は`discovered`のまま変更せず、`exclude_reason`にエラー内容だけ記録。次回`enrichSites`を再実行すれば`website_url`はキャッシュ済みなので検索からやり直さずに再試行できる設計にした。
  - 正常に取得・解析できた → `status=enriched`。
- **メールアドレスの採否は「正規表現での本文抽出」を「AIの`contact_type`判定」より優先**する設計にした。理由：AIの言い間違い・幻覚より、本文に実在する文字列を正規表現で拾う方が確実なため。AIには本文にメールが無い場合の`contact_type`（form_only/none）判定と、事業要約・お断り表示判定のみを任せている。
- **スクレイピングのマナー実装**：`SCRAPE_DELAY_MS`（既定3000ms）を挟むペーサーを、同一サイト内の複数ページ取得・会社間の両方に適用。cheerioでの静的取得で本文がほぼ空（SPAのルート要素のみ等）と判定された場合のみPlaywrightにフォールバックし、常時ブラウザを起動しない設計にした。
- **テストは実ネットワーク・実AI呼び出しに依存しない部分のみ自動化した**（32件、全てパス）：robots.txtパース・isAllowedByRobots（fetchImplを注入）、メール抽出の正規表現・誤検出フィルタ、会社概要/お問い合わせリンク検出、SPA判定、`enrichSites`のオーケストレーション全分岐（find/fetch/analyze/isAllowedを注入したフェイクで検証）。Anthropic APIの実呼び出し（Web検索・要約）は自動テスト化していない（APIキー・実サイトへの課金を伴うネットワークアクセスが必要なため）。
- **実サイトでのライブ動作確認をユーザー承認の上で1件実施した**（サイボウズ株式会社）。社名＋所在地から公式サイト（cybozu.co.jp）を正しく特定し、robots.txt確認→本文取得→事業要約・お断り判定まで一連の流れが動作することを確認済み。Playwright（chromium）自体の起動もローカルで別途スモークテスト済み。
- filterCompliant（M3）は未実装。次回以降のセッションで着手する。

## 2026-07-01: corp-lead-kit M3実装（適格判定・除外／特電法フィルタ）

- **判定順序を「安全側チェック→重複名寄せ→連絡手段の分類」に固定した**。理由：`optout_notice`（お断り表示）とサプレッションリスト一致は法的に絶対に譲れないため最優先で判定し、この2つで除外された会社は重複名寄せの対象から外す（=重複していたという理由で「お断りされた」という事実が握りつぶされないようにする）。重複名寄せの後に初めてemail/contact_typeでmail_ready/call_list/no_contactを決める。
- **重複グループの生存者選定は「email保有 > form_only > none」の優先順位にした**。理由：同一ドメイン（グループ会社が1サイトを共有している等）や同一法人番号が重複した場合、どうせ1社しか残せないなら最も連絡手段の質が高い（=直接メールできる）record を残す方が歩留まりが上がるため。同順位内では配列内で先に出てきた方を残す（決定的な挙動にするため）。
- **同一法人番号の重複は、DBの`corporate_no UNIQUE`制約により実運用ではまず発生しない**（M1のUPSERTで1法人番号=1行に潰れる）。それでも仕様書に明記されたルールのため実装・テストは行った。テストでは異なる法人番号で登録した2社に対し、呼び出し側の配列上でのみ`corporate_no`を同一値に上書きして重複状態を模擬している（DB制約を回避するテスト用のトリックであり、実データでこの経路が通ることは基本的にない）。
- **`status !== 'enriched'` の会社はM3の判定対象外とし、そのまま素通りさせる設計にした**。理由：M2でサイトが見つからず`excluded`になった会社や、一時エラーで`discovered`のまま残っている会社にM3が誤って`no_contact`等のラベルを上書きしないようにするため。M3は「M2が完了した会社」だけを判定する。
- **`addToSuppressionList`をこのタイミングで`lib/db.js`に追加したが、`index.js`の公開APIにはまだ出していない**。理由：サプレッションへの書き込みはM7（返信・配信停止管理、auto_apo-app側）の役割であり、今回のスコープ（filterCompliantの実装）ではない。ただし書き込み手段が無いとサプレッション一致除外が実運用で一生発火しないため、最小限の内部ヘルパーとしてだけ用意した。M7実装時に配線する。
