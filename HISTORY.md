# HISTORY

コード・Gitでは追えない「なぜそうなったか」を記録する。詳細仕様は `auto_apo-app_仕様書_v1.md` を参照。

## 2026-07-01: corp-lead-kit 初期実装（M1：CSV取り込み＋DB）

- **依存ライブラリを一切追加していない**。`node -v` が v24.15.0（フルICU入り）だったため、`node:sqlite`（DatabaseSync）と `TextDecoder('shift-jis')` がいずれも標準機能で動作することを確認済み。iconv-lite等のShift-JIS変換ライブラリ、CSVパーサライブラリは不要と判断。
- **CSVの行分割は生バイトのLF(0x0A)単位で行い、その後にデコードする方式にした**。理由：LF・CR・カンマ・引用符（いずれも0x00〜0x3F/0x7Fの範囲）は、Shift-JISの2バイト文字の後続バイト範囲（0x40〜0xFC）・UTF-8の継続バイト範囲（0x80〜0xBF）のどちらにも含まれないため、デコード前に生バイトで区切っても文字の途中で切れることがない。これにより219MB級の全件CSVでも先頭から`--limit`件で早期終了でき、全件をメモリに載せる必要がない。
- **ZIP展開・OpenPGP署名検証はv1では実装していない**。国税庁の全件ダウンロードはZIP圧縮＋署名付きだが、展開・検証には追加ライブラリが必要になるため保留。現状は「ユーザーが手動でZIPを展開した後の.csvファイルを渡す」運用とする。必要になれば別途相談の上、ライブラリ追加を検討する。
- **テストのCSVフィクスチャはUTF-8で作成した**。Node標準のTextEncoderはUTF-8エンコードしかできず、Shift-JISへの変換ライブラリなしにShift-JISのテストデータを生成できないため。Shift-JISデコード自体は`TextDecoder('shift-jis').decode(...)`の単体確認で動作を確認済み（実運用でShift-JIS版CSVを使う場合は`encoding: 'shift-jis'`を指定すれば動く設計）。
- **列がズレた場合は即座にthrowして処理全体を中断する設計にした**（仕様書§3の指示通り）。法人番号13桁・都道府県コード1〜2桁・郵便番号7桁の自己検証をパース時に行い、不一致ならその場でエラーを投げる。サイレントに壊れたデータをDBに入れないことを優先。
- M2（サイト巡回）・M3（適格判定）・auto_apo-app（M4以降）は未実装。次回以降のセッションで着手する。
